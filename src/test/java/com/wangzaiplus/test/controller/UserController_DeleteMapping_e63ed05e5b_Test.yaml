# Test generated by RoostGPT for test springboot using AI Type Open AI and AI Model gpt-4

config:
  target: "http://api.dummy.com"
  phases:
    - duration: 60
      arrivalRate: 20

scenarios:
  - name: "Successful API request"
    flow:
      - delete:
          url: "/user/{{ $randomNumber(100, 1000) }}"
          expect:
            - statusCode: 200
            - body: "nice"
            
  - name: "API request with validation failure"
    flow:
      - delete:
          url: "/user/invalid_id"
          expect:
            - statusCode: 400
            
  - name: "API request with edge case"
    flow:
      - delete:
          url: "/user/{{ $randomNumber(100000000, 1000000000) }}"
          expect:
            - statusCode: 404
