# Test generated by RoostGPT for test artillery-spec using AI Type Open AI and AI Model gpt-4

config:
  target: "{{ $processEnvironment.SERVER_URL }}/fund"
  phases:
    - duration: 60
      arrivalRate: 20
  payload:
    path: "FundController_PostMapping_f1bd7cfb2f_Test.csv"
    fields:
      - "fundDto"
    skipHeader: true
  plugins:
    expect: {}
    
scenarios:
  - name: 'Successful API request'
    flow:
      - post:
          url: "/rank"
          json:
            fundDto: "{{ fundDto }}"
          headers:
            Content-Type: "application/json"
          expect:
            - statusCode: 200
            - hasProperty: 'data'
            - hasProperty: 'message'
            
  - name: 'API request with validation failure'
    flow:
      - post:
          url: "/invalidurl"
          json:
            fundDto: "{{ fundDto }}"
          headers:
            Content-Type: "application/json"
          expect:
            - statusCode: 400
            - hasProperty: 'error'
            
  - name: 'API request with edge case'
    flow:
      - post:
          url: "/rank"
          json:
            fundDto: "SpecialCharactersOrLargeData{{ fundDto }}"
          headers:
            Content-Type: "application/json"
          expect:
            - statusCode: "{{ $between(200, 500) }}"
            - hasProperty: 'data'
            - hasProperty: 'message'
