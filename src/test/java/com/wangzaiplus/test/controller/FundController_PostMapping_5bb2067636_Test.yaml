# Test generated by RoostGPT for test artillery-spec using AI Type Open AI and AI Model gpt-4

config:
  target: "{{ $processEnvironment.SERVER_URL }}/fund"
  plugins:
    expect: {}
  payload:
    path: "FundController_PostMapping_5bb2067636_Test.csv"
    fields:
      - "type"
    skipHeader: true

scenarios:
  - name: "Successful API request"
    flow:
      - post:
          url: "/getFundDtoList"
          json:
            type: "{{ type }}"
          headers:
            Content-Type: "application/json"
          expect:
            - statusCode: 200
            - hasProperty: "success"

  - name: "API request with validation failure"
    flow:
      - post:
          url: "/invalidURL"
          json:
            type: "{{ type }}"
          headers:
            Content-Type: "application/json"
          expect:
            - statusCode: 400

  - name: "API request with edge case"
    flow:
      - post:
          url: "/getFundDtoList"
          json:
            type: "{{ type }}"
          headers:
            Content-Type: "application/json"
          expect:
            - statusCode: 200
            - hasProperty: "success"
