# Test generated by RoostGPT for test springboot using AI Type Open AI and AI Model gpt-4

config:
  target: "http://localhost:8080"
  phases:
    - duration: 60
      arrivalRate: 20
scenarios:
  - name: "Successful API request"
    flow:
      - get:
          url: "/testRedisson"
          expect:
            - statusCode: 200
            - body: "ok......................................."

  - name: "API request with failure"
    flow:
      - get:
          url: "/testRedisson"
          expect:
            - statusCode: 200
            - body: "获取锁失败......................................."

  - name: "API request with exception"
    flow:
      - get:
          url: "/testRedisson"
          expect:
            - statusCode: 200
            - body: "获取锁异常......................................."

  - name: "API request with unlock failure"
    flow:
      - get:
          url: "/testRedisson"
          expect:
            - statusCode: 200
            - body: "获取锁失败"
