# Test generated by RoostGPT for test artillery-spec using AI Type Open AI and AI Model gpt-4

config:
  target: "{{ $processEnvironment.SERVER_URL }}/fund"
  phases:
    - duration: 60
      arrivalRate: 20
  payload:
    path: "FundController_PostMapping_d24e9c0fd7_Test.csv"
    fields: ["field1", "field2", "field3"]
    skipHeader: true
  plugins:
    expect: {}

scenarios:
  - name: "Successful API request"
    flow:
      - post:
          url: "/search"
          json:
            field1: "{{ field1 }}"
            field2: "{{ field2 }}"
            field3: "{{ field3 }}"
          headers:
            Content-Type: "application/json"
          expect:
            - statusCode: 200
            - hasProperty: "result"
            - hasHeader: "content-type"

  - name: "API request with validation failure"
    flow:
      - post:
          url: "/search"
          json:
            field1: ""
            field2: "{{ field2 }}"
            field3: "{{ field3 }}"
          headers:
            Content-Type: "application/json"
          expect:
            - statusCode: 400
            - hasProperty: "error"
            - hasHeader: "content-type"

  - name: "API request with edge case"
    flow:
      - post:
          url: "/search"
          json:
            field1: "{{ field1 }}"
            field2: "{{ field2 }}"
            field3: "This is a very large string to test the edge case scenario"
          headers:
            Content-Type: "application/json"
          expect:
            - statusCode: 200
            - hasProperty: "result"
            - hasHeader: "content-type"
