# Test generated by RoostGPT for test springboot using AI Type Open AI and AI Model gpt-4

config:
  target: "http://dummyapi.com"
  phases:
    - duration: 60
      arrivalRate: 20
scenarios:
  - name: "Successful API request"
    flow:
      - post:
          url: "/login"
          json:
            username: "validUser"
            password: "validPassword"
          headers:
            Content-Type: "application/json"
          capture:
            json: "$"
            as: "response"
      - think: 5
      - log: "Response: {{ response }}"
      - assert:
          - statusCode: 200
          - bodyContains:
              - "Login successful"
  - name: "API request with validation failure"
    flow:
      - post:
          url: "/login"
          json:
            username: "invalidUser"
          headers:
            Content-Type: "application/json"
          capture:
            json: "$"
            as: "response"
      - think: 5
      - log: "Response: {{ response }}"
      - assert:
          - statusCode: 400
          - bodyContains:
              - "Invalid request"
  - name: "API request with edge case"
    flow:
      - post:
          url: "/login"
          json:
            username: "userWithSpecialChars@#"
            password: "veryLongPasswordThatExceedsTheLimit"
          headers:
            Content-Type: "application/json"
          capture:
            json: "$"
            as: "response"
      - think: 5
      - log: "Response: {{ response }}"
      - assert:
          - statusCode: [{"in": [200, 400]}]
          - bodyContains:
              - "Login successful"
              - "Invalid request"
