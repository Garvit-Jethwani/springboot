# Test generated by RoostGPT for test artillery-spec using AI Type Open AI and AI Model gpt-4

config:
  target: "{{ $processEnvironment.SERVER_URL }}/fund"
  plugins:
    expect: {}
  payload:
    path: "FundController_PostMapping_b4ba742b48_Test.csv"
    fields: ["field1", "field2", "field3"]
    skipHeader: true

scenarios:
  - name: "Successful API request"
    flow:
      - post:
          url: "/getSearchFormDto"
          headers:
            Content-Type: "application/json"
          json:
            field1: "{{ field1 }}"
            field2: "{{ field2 }}"
            field3: "{{ field3 }}"
          expect:
            - statusCode: 200
            - hasProperty: "data"
            - hasHeader: "content-type"

  - name: "API request with validation failure"
    flow:
      - post:
          url: "/getSearchFormDto"
          headers:
            Content-Type: "application/json"
          json:
            field1: "{{ field1 }}"
            field2: "{{ field2 }}"
          expect:
            - statusCode: 400
            - hasProperty: "error"
            - hasHeader: "content-type"

  - name: "API request with edge case"
    flow:
      - post:
          url: "/getSearchFormDto"
          headers:
            Content-Type: "application/json"
          json:
            field1: "{{ field1 }}"
            field2: "{{ field2 }}"
            field3: "{{ field3 }}"
          expect:
            - statusCode: 200
            - hasProperty: "data"
            - hasHeader: "content-type"
            - body: "The quick brown fox jumps over the lazy dog"
